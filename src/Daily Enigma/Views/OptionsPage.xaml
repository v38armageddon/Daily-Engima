<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Daily_Enigma.Views.OptionsPage"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:Daily_Enigma.ViewModels"  
             xmlns:model="clr-namespace:Daily_Enigma.Models"  
             x:Name="BrowseItemsPage">

    <Grid BackgroundColor="#434343">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ScrollView Grid.Row="1">
            <StackLayout Orientation="Vertical" Padding="30,24,30,24" Spacing="10">
                <Label Text="Notification" FontSize="Title"/>
                <Label Text="Receive notifications for new enigma (need permission) " FontSize="Subtitle"/>
                <Switch IsToggled="False"/>
            </StackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
